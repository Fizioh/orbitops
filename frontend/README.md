# OrbitOps frontend

React + TypeScript (strict) + Vite. Feature folders own UI, API clients, and types.
Authoritative orbital / scheduling / feasibility logic remains on the backend.

## Scripts

```bash
npm install
npm run dev
npm run test
npm run lint
npm run typecheck
npm run build
```
